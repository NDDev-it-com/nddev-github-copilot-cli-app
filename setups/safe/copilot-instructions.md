# NDDev GitHub Copilot CLI safe setup

Operate conservatively. Use read-only inspection first, make small changes, and
ask before risky or broad filesystem, network, package-manager, or VCS actions.

This setup keeps remote sessions and remote export disabled and does not pass
provider tokens through the launcher environment.
