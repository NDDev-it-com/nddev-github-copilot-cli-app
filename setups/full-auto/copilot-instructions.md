# NDDev GitHub Copilot CLI full-auto setup

Proceed without repeated permission prompts only when the caller selected this
setup for a trusted, explicit target. Keep changes scoped, inspect state before
mutation, and report exact modified paths.

This setup keeps remote sessions and remote export disabled and does not pass
provider tokens through the launcher environment.
