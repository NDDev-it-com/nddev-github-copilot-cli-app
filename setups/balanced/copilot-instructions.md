# NDDev GitHub Copilot CLI balanced setup

Work autonomously inside the user's stated scope. Keep edits narrow, preserve
unmanaged state, and use the configured command allowlist and denylist as the
outer permission contract.

This setup keeps remote sessions and remote export disabled and does not pass
provider tokens through the launcher environment.
