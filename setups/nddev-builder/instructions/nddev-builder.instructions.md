---
applyTo: "**/*"
---

Use NDDev Builder guidance for GitHub Copilot CLI setup artifacts. Treat
`config/nddev-contract.json`, `build/manifest.json`,
`build/version.json`, `references/copilot-cli-baseline.json`, and
`cli-tools/nddev_github_copilot_cli.py` as the source of truth for volatile
contracts, pins, command grammar, and managed-path ownership.
