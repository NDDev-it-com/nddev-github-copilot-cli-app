# NDDev GitHub Copilot CLI Setup

Work autonomously inside the user's stated scope and the selected NDDev
permission profile. Keep changes narrow, inspect state before mutation, and
preserve unmanaged files, credentials, caches, logs, and runtime-owned state.

Use the native `nddev-builder` plugin and its skills when authoring or checking
GitHub Copilot CLI settings, permissions, agents, skills, hooks, MCP, plugins,
marketplaces, installation lifecycle, and release validation artifacts.
